# Release candidate f9dc7a8 — 2026-09-21

## Decision

**Hold.** Draft [pull request 19](https://github.com/ny147/mdez/pull/19) contains the implemented Preview isolation and release-verification gates. The candidate is not merged or promoted. Operator-owned hosting, Production database, repository-protection, and final hosted smoke evidence remain open.

Application candidate SHA: `f9dc7a8b36f6a8c54e7dd3401c5b7a1988f7de6f`  
Task branch: `codex/deployment-release`

## Automated gates

| Gate | Result | Evidence |
| --- | --- | --- |
| Verify | Passed | [CI run 35602609742](https://github.com/ny147/mdez/actions/runs/35602609742), 4m57s |
| Preview boundary | Passed | Same run, desktop and mobile projects, 1m13s |
| Database integration | Passed | Same run, PostgreSQL 17, five files and seven tests, 49s |
| Database negative control | Rejected the seeded regression | [CI run 35510132439](https://github.com/ny147/mdez/actions/runs/35510132439) failed the deliberately inverted cleanup assertion; the assertion was then restored |

Local verification passed lint, type checking, build, 365 unit tests, and 198 end-to-end tests with six Preview-only cases skipped in the normal environment. The isolated Preview suite passed four cases. A local PostgreSQL rerun was unavailable because Docker Desktop failed at its host-level inference socket and `psql` was not installed; the successful disposable-PostgreSQL CI job is the database evidence.

## Hosted Preview evidence

The candidate deployment `CTEPmgAhcKYWBicCB2ynfTF8aJVe` was tested at `https://mdez-rkiaa8oft-ny147s-projects.vercel.app/`. Vercel Deployment Protection required authentication.

At 1440×900, a synthetic local-only page passed:

- Markdown edit, autosave, reload, and persistence;
- reader rendering for a list, TypeScript code block, and math;
- theme switching, body search, bookmark, and resume;
- Markdown export;
- public GitHub preview and local import from `octocat/Spoon-Knife`;
- direct `/share/abc` unavailable-state rendering.

The browser-control session lost focus after dismissing GitHub refresh's overwrite confirmation, so preservation after that dismissal was not re-inspected. Hosted 390px testing also remains open: authenticated Chrome did not apply the requested viewport, and the controllable in-app browser could not cross Vercel authentication. No hosted sharing or group record was created.

## Open release gates

- [ ] Resolve the canonical Production alias/project/deployment/SHA and duplicate Vercel project identity.
- [ ] Verify runtime, build, branch, domain, Deployment Protection, and Preview environment scopes; remove Production credentials from Preview only with explicit authorization.
- [ ] Inventory older Preview deployments for credential exposure.
- [ ] Match the CI PostgreSQL major version to Production and verify migrations, pooling, backup, and recovery.
- [ ] Require `Verify`, `Preview boundary`, and `Database integration` in branch protection.
- [ ] Complete authenticated hosted-mobile smoke testing and the remaining GitHub failure-preservation check.
- [ ] With action-time approval, submit synthetic Preview Quick Share and Key Group requests to confirm the boundary without creating public records.
- [ ] Record an authorized Production synthetic sharing smoke, scheduler history, rollback target, and manual promotion evidence.

The user merges and promotes manually. Do not enable auto-merge, delete the task branch, or treat this record as release approval.
